#image: python:2.7
#services:
#  - postgres:9.5

stages:
  - test
  - deploy

test:
  type: test
  script:
  # this configures Django application to use attached postgres database that is run on `postgres` host
  # - export DATABASE_URL=postgres://postgres:@postgres:5432/atlas-api-db
    - apt-get update -qy
    - apt-get install -y python-dev python-pip
    - pip install -r requirements.txt
    - python manage.py test

# This environment contains stage release candidate code
stage deployment:
  type: deploy
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=$HEROKU_DEV_APPNAME --api-key=$HEROKU_API_KEY
  only:
  - stage-rc

# This environment is an exact replica of production and is used to patch
hotfix deployment:
  type: deploy
  script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=$HEROKU_PROD_COPY_APPNAME --api-key=$HEROKU_API_KEY
  only:
  - master

# TODO: READ >> https://gitlab.com/help/ci/yaml/README.md#environment
